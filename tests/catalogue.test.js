const request = require("supertest");
const app = require("../index");

describe("CATALOGUES", () => {
	it("should get your catalogue", () => {});

	it("should be able to add a movie to your catalogue", () => {});

	it("should be able to remove a movie from your catalogue", () => {});
});
